---
plan: 03-03
phase: 03
title: Matching Pairs Activity + Gap-Fill Activity + Sentence Data
wave: 2
depends_on: [03-01]
autonomous: true
files_modified:
  - js/exercises.js
  - data/exercises/gap-fill.json
requirements:
  - EX-05
  - EX-09
---

## Objective

Implement two context-rich activities: matching pairs (vocabulary in context) and gap-fill sentences (CE-level Latin with one blanked word). Both extend `js/exercises.js` and use real vocabulary from `data/vocabulary/all.json`. The gap-fill activity also needs a new `data/exercises/gap-fill.json` data file.

## Background

CE Question 1 (comprehension) and Question 2 (translation) both require pupils to understand vocabulary **in context** — not just isolated word pairs. These two activities bridge the gap between flashcard-style recall and full sentence work.

Cognitive design principles:
- Matching pairs: pairs are chosen from the same topic to increase meaningful association
- Gap-fill: sentence context gives clues, making it a productive struggle not a guessing game
- Both activities: feedback explains the grammar behind the correct answer

---

## Task 1: Create data/exercises/gap-fill.json

**Files:** `data/exercises/gap-fill.json` (new file)

**Action:**
Create a JSON array of gap-fill sentences. Each sentence is CE-level Latin with one blanked content word. 4 options given — correct answer + 3 plausible distractors.

Schema:
```json
{
  "id": "gf-01",
  "sentence": "puella in _____ ambulat.",
  "blank_word": "via",
  "translation_with_blank": "The girl walks in the _____.",
  "options": ["via", "aqua", "silva", "porta"],
  "correct": "via",
  "explanation": "'via' (road/street) is the ablative after 'in' meaning 'in/on'. The girl walks in/along the street."
}
```

Write at least 20 sentences covering a range of topics (family, daily life, war/army, nature, travel) and using vocabulary from all.json. Sentences should be CE Level 1–2 difficulty: short (5–10 words), one main verb, familiar vocabulary.

```json
[
  {
    "id": "gf-01",
    "sentence": "puella in viā ambulat.",
    "blank_word": "viā",
    "display": "puella in _____ ambulat.",
    "translation": "The girl walks in the street.",
    "options": ["viā", "silvā", "aquā", "portā"],
    "correct": "viā",
    "explanation": "'viā' (road/street, ablative) follows 'in'. The girl walks in/along the street."
  },
  {
    "id": "gf-02",
    "sentence": "servus cibum portat.",
    "blank_word": "cibum",
    "display": "servus _____ portat.",
    "translation": "The slave carries the food.",
    "options": ["cibum", "aquam", "pecūniam", "vinum"],
    "correct": "cibum",
    "explanation": "'cibum' (food, accusative) is the direct object of 'portat' (carries). The accusative marks the thing being carried."
  },
  {
    "id": "gf-03",
    "sentence": "dominus servōs laudat.",
    "blank_word": "servōs",
    "display": "dominus _____ laudat.",
    "translation": "The master praises the slaves.",
    "options": ["servōs", "servī", "servō", "servus"],
    "correct": "servōs",
    "explanation": "'servōs' is accusative plural — the slaves are the object of 'laudat' (praises)."
  },
  {
    "id": "gf-04",
    "sentence": "māter fīliōs amat.",
    "blank_word": "fīliōs",
    "display": "māter _____ amat.",
    "translation": "The mother loves her sons.",
    "options": ["fīliōs", "fīliī", "fīliō", "fīlius"],
    "correct": "fīliōs",
    "explanation": "'fīliōs' is accusative plural — the sons are the object of 'amat' (loves)."
  },
  {
    "id": "gf-05",
    "sentence": "mīlitēs in oppidō manēbant.",
    "blank_word": "oppidō",
    "display": "mīlitēs in _____ manēbant.",
    "translation": "The soldiers were staying in the town.",
    "options": ["oppidō", "oppidum", "oppidī", "oppida"],
    "correct": "oppidō",
    "explanation": "'oppidō' is ablative singular after 'in' (meaning location: in the town). The imperfect 'manēbant' = they were staying."
  },
  {
    "id": "gf-06",
    "sentence": "puer librōs legit.",
    "blank_word": "librōs",
    "display": "puer _____ legit.",
    "translation": "The boy reads the books.",
    "options": ["librōs", "librī", "librō", "liber"],
    "correct": "librōs",
    "explanation": "'librōs' is accusative plural — the books are the direct object of 'legit' (reads)."
  },
  {
    "id": "gf-07",
    "sentence": "nauta per aquam nāvigābat.",
    "blank_word": "aquam",
    "display": "nauta per _____ nāvigābat.",
    "translation": "The sailor was sailing through the water.",
    "options": ["aquam", "aquā", "aquae", "aquārum"],
    "correct": "aquam",
    "explanation": "'aquam' is accusative after 'per' (through). The preposition 'per' always takes the accusative."
  },
  {
    "id": "gf-08",
    "sentence": "fēmina pecūniam habet.",
    "blank_word": "pecūniam",
    "display": "fēmina _____ habet.",
    "translation": "The woman has money.",
    "options": ["pecūniam", "pecūniā", "pecūniae", "pecūniārum"],
    "correct": "pecūniam",
    "explanation": "'pecūniam' is accusative — the money is the direct object of 'habet' (has)."
  },
  {
    "id": "gf-09",
    "sentence": "imperātor mīlitēs laudāvit.",
    "blank_word": "mīlitēs",
    "display": "imperātor _____ laudāvit.",
    "translation": "The general praised the soldiers.",
    "options": ["mīlitēs", "mīlitibus", "mīlitum", "mīlitī"],
    "correct": "mīlitēs",
    "explanation": "'mīlitēs' is accusative plural — the soldiers are the object of 'laudāvit' (praised, perfect tense)."
  },
  {
    "id": "gf-10",
    "sentence": "puellae in hortō lūdēbant.",
    "blank_word": "hortō",
    "display": "puellae in _____ lūdēbant.",
    "translation": "The girls were playing in the garden.",
    "options": ["hortō", "hortum", "hortī", "hortus"],
    "correct": "hortō",
    "explanation": "'hortō' is ablative after 'in' (location). The imperfect 'lūdēbant' = they were playing."
  },
  {
    "id": "gf-11",
    "sentence": "pater fīliae dōnum dedit.",
    "blank_word": "fīliae",
    "display": "pater _____ dōnum dedit.",
    "translation": "The father gave a gift to his daughter.",
    "options": ["fīliae", "fīliam", "fīliā", "fīliārum"],
    "correct": "fīliae",
    "explanation": "'fīliae' is dative singular — she is the indirect object (the gift is given *to* her). Dative = the person something is done for."
  },
  {
    "id": "gf-12",
    "sentence": "cīvēs in forō stābant.",
    "blank_word": "forō",
    "display": "cīvēs in _____ stābant.",
    "translation": "The citizens were standing in the forum.",
    "options": ["forō", "forum", "forī", "fora"],
    "correct": "forō",
    "explanation": "'forō' is ablative after 'in' (location — standing *in* the forum). If movement were involved, the accusative 'forum' would be used."
  },
  {
    "id": "gf-13",
    "sentence": "rēx cum mīlitibus pugnat.",
    "blank_word": "mīlitibus",
    "display": "rēx cum _____ pugnat.",
    "translation": "The king fights with the soldiers.",
    "options": ["mīlitibus", "mīlitēs", "mīlitum", "mīlitem"],
    "correct": "mīlitibus",
    "explanation": "'mīlitibus' is ablative plural after 'cum' (with). The preposition 'cum' always takes the ablative."
  },
  {
    "id": "gf-14",
    "sentence": "ancilla dominam timet.",
    "blank_word": "dominam",
    "display": "ancilla _____ timet.",
    "translation": "The slave-girl fears the mistress.",
    "options": ["dominam", "domina", "dominae", "dominō"],
    "correct": "dominam",
    "explanation": "'dominam' is accusative — the mistress is the object of 'timet' (fears). The subject 'ancilla' stays nominative."
  },
  {
    "id": "gf-15",
    "sentence": "poēta carmina scrīpsit.",
    "blank_word": "carmina",
    "display": "poēta _____ scrīpsit.",
    "translation": "The poet wrote songs/poems.",
    "options": ["carmina", "carminum", "carminibus", "carminī"],
    "correct": "carmina",
    "explanation": "'carmina' is accusative plural (neuter) — the poems are the object of 'scrīpsit' (wrote, perfect tense)."
  },
  {
    "id": "gf-16",
    "sentence": "servī in agrīs labōrābant.",
    "blank_word": "agrīs",
    "display": "servī in _____ labōrābant.",
    "translation": "The slaves were working in the fields.",
    "options": ["agrīs", "agrōs", "agrī", "agrum"],
    "correct": "agrīs",
    "explanation": "'agrīs' is ablative plural after 'in' (location). The imperfect 'labōrābant' = they were working (continuous past)."
  },
  {
    "id": "gf-17",
    "sentence": "deus Rōmānōs servāvit.",
    "blank_word": "Rōmānōs",
    "display": "deus _____ servāvit.",
    "translation": "The god saved the Romans.",
    "options": ["Rōmānōs", "Rōmānī", "Rōmānīs", "Rōmānus"],
    "correct": "Rōmānōs",
    "explanation": "'Rōmānōs' is accusative plural — the Romans are the object of 'servāvit' (saved, perfect tense)."
  },
  {
    "id": "gf-18",
    "sentence": "magister discipulīs respondēbat.",
    "blank_word": "discipulīs",
    "display": "magister _____ respondēbat.",
    "translation": "The teacher was answering the pupils.",
    "options": ["discipulīs", "discipulōs", "discipulī", "discipulum"],
    "correct": "discipulīs",
    "explanation": "'discipulīs' is dative plural — 'respondēre' (to answer/reply) takes the dative for the person replied to."
  },
  {
    "id": "gf-19",
    "sentence": "nāvis ad portum nāvigāvit.",
    "blank_word": "portum",
    "display": "nāvis ad _____ nāvigāvit.",
    "translation": "The ship sailed to the harbour.",
    "options": ["portum", "portō", "portī", "portus"],
    "correct": "portum",
    "explanation": "'portum' is accusative after 'ad' (to/towards). The preposition 'ad' always takes the accusative."
  },
  {
    "id": "gf-20",
    "sentence": "puella aquam ex fonte portāvit.",
    "blank_word": "fonte",
    "display": "puella aquam ex _____ portāvit.",
    "translation": "The girl carried water from the spring.",
    "options": ["fonte", "fontem", "fontis", "fontibus"],
    "correct": "fonte",
    "explanation": "'fonte' is ablative after 'ex' (from/out of). The preposition 'ex' always takes the ablative."
  }
]
```

**Verify:** `data/exercises/gap-fill.json` is valid JSON with 20 sentences, each with `display`, `correct`, `options` (4), and `explanation`.

**Done:** Gap-fill sentence data created with 20 CE-level sentences, real Latin, grammatical explanations.

---

## Task 2: Add matching pairs activity to exercises.js

**Files:** `js/exercises.js` (extend)

**Action:**
Add `runMatchingPairs(app)` function. Load `data/vocabulary/all.json`, pick one topic at random (or use `?filter=topic:family`), take 6 words from that topic, shuffle the English meanings separately. Present two columns: Latin words on left, English meanings on right (shuffled). Pupil taps a Latin word (highlights it), then taps its English meaning.

Implementation:

```javascript
function runMatchingPairs(app) {
  fetch('data/vocabulary/all.json')
    .then(r => r.json())
    .then(words => {
      // Pick a topic
      const params = new URLSearchParams(location.search);
      const topicFilter = params.get('filter');
      let topic = topicFilter ? topicFilter.replace('topic:', '') : null;
      const topics = ['family', 'daily-life', 'war-army', 'nature', 'travel', 'gods'];
      if (!topic || !topics.includes(topic)) {
        topic = topics[Math.floor(Math.random() * topics.length)];
      }

      // Filter and take 6
      const pool = words.filter(w => w.topics && w.topics.includes(topic) && w.part_of_speech === 'noun');
      if (pool.length < 6) {
        // Fallback: take any 6 nouns
        const allNouns = words.filter(w => w.part_of_speech === 'noun');
        return startPairs(shuffle(allNouns).slice(0, 6), app);
      }
      const selected = shuffle(pool).slice(0, 6);
      startPairs(selected, app, topic);
    })
    .catch(() => {
      app.innerHTML = '<p class="error">Could not load vocabulary data.</p>';
    });
}

function startPairs(words, app, topic) {
  const topicLabel = topic ? topic.replace('-', '/') : 'mixed';
  let matched = new Set();
  let selectedLatin = null;

  const latinIds = words.map((w, i) => `latin-${i}`);
  const englishItems = shuffle(words.map((w, i) => ({ id: i, english: w.english.split(',')[0].trim() })));

  function render() {
    const latinCol = words.map((w, i) => {
      const isMatched = matched.has(i);
      const isSelected = selectedLatin === i;
      return `<button class="pair-btn latin-btn${isMatched ? ' matched' : ''}${isSelected ? ' selected' : ''}"
        data-idx="${i}" ${isMatched ? 'disabled' : ''}>${w.latin}</button>`;
    }).join('');

    const englishCol = englishItems.map(item => {
      const isMatched = matched.has(item.id);
      return `<button class="pair-btn english-btn${isMatched ? ' matched' : ''}"
        data-idx="${item.id}" ${isMatched ? 'disabled' : ''}>${item.english}</button>`;
    }).join('');

    app.innerHTML = `
      <div class="ex-header">
        <a href="quiz.html" class="ex-back">← Exercises</a>
        <span class="ex-ce-label">Practises: CE vocabulary recall</span>
      </div>
      <h2>Matching Pairs</h2>
      <p class="ex-topic-label">Topic: <strong>${topicLabel}</strong> — tap a Latin word, then its English meaning</p>
      <div class="pairs-grid">
        <div class="pairs-col">${latinCol}</div>
        <div class="pairs-col">${englishCol}</div>
      </div>
      <p class="pairs-score">${matched.size}/6 matched</p>
    `;

    // Attach events
    app.querySelectorAll('.latin-btn:not([disabled])').forEach(btn => {
      btn.addEventListener('click', () => {
        selectedLatin = parseInt(btn.dataset.idx);
        render();
      });
    });

    app.querySelectorAll('.english-btn:not([disabled])').forEach(btn => {
      btn.addEventListener('click', () => {
        if (selectedLatin === null) return;
        const englishIdx = parseInt(btn.dataset.idx);
        if (selectedLatin === englishIdx) {
          // Correct match
          matched.add(selectedLatin);
          selectedLatin = null;
          render();
          if (matched.size === 6) showPairsSummary(app, words, topic);
        } else {
          // Wrong — flash both buttons red briefly, deselect
          btn.classList.add('wrong');
          const latinBtn = app.querySelector(`.latin-btn[data-idx="${selectedLatin}"]`);
          if (latinBtn) latinBtn.classList.add('wrong');
          setTimeout(() => { selectedLatin = null; render(); }, 800);
        }
      });
    });
  }

  render();
}

function showPairsSummary(app, words, topic) {
  const topicLabel = topic ? topic.replace('-', '/') : 'mixed';
  app.innerHTML = `
    <div class="ex-summary">
      <div class="ex-summary-icon">✓</div>
      <h2>All matched!</h2>
      <p>You matched all 6 ${topicLabel} words correctly.</p>
      <div class="ex-summary-actions">
        <a href="quiz.html?activity=pairs" class="ex-btn ex-btn-primary">Try another set</a>
        <a href="quiz.html" class="ex-btn ex-btn-secondary">Back to Exercises</a>
      </div>
    </div>
  `;
}
```

Add route in activity switch:
```javascript
case 'pairs': return runMatchingPairs(app);
```

Also add to hub card list in `renderHub()`:
```javascript
{
  id: 'pairs',
  title: 'Matching Pairs',
  desc: 'Match 6 Latin words with their English meanings.',
  ce: 'Practises: CE vocabulary recall',
  href: 'quiz.html?activity=pairs',
  icon: '🔗'
}
```

**Verify:**
- `quiz.html?activity=pairs` shows two columns of 6 buttons each
- Tapping a Latin word highlights it; tapping its English match locks both green
- Tapping the wrong English match flashes both red and resets
- All 6 matched shows success summary

**Done:** Matching pairs activity functional, choosing random topic, full pairing interaction with correct/wrong feedback.

---

## Task 3: Add gap-fill activity to exercises.js

**Files:** `js/exercises.js` (extend)

**Action:**
Add `runGapFill(app)` function. Load `data/exercises/gap-fill.json`. Shuffle and take 8 sentences. For each: show the sentence with `_____` displayed, 4 option buttons, immediate feedback with `explanation` text.

```javascript
function runGapFill(app) {
  fetch('data/exercises/gap-fill.json')
    .then(r => r.json())
    .then(data => {
      const questions = shuffle(data).slice(0, 8);
      let idx = 0, correct = 0;

      function showQuestion() {
        if (idx >= questions.length) return showGapFillSummary(app, correct, questions.length);
        const q = questions[idx];
        const opts = shuffle([...q.options]);
        app.innerHTML = `
          <div class="ex-header">
            <a href="quiz.html" class="ex-back">← Exercises</a>
            <span class="ex-ce-label">Practises: CE Questions 1 & 2 (vocabulary in context)</span>
          </div>
          <p class="ex-progress">${idx + 1} / ${questions.length}</p>
          <div class="ex-sentence-card">
            <p class="ex-sentence">${q.display}</p>
            <p class="ex-translation-hint">${q.translation.replace(q.correct, '_____')}</p>
          </div>
          <div class="ex-options">
            ${opts.map(o => `<button class="ex-opt-btn" data-val="${o}">${o}</button>`).join('')}
          </div>
          <div class="ex-feedback" id="gf-feedback" style="display:none"></div>
        `;

        app.querySelectorAll('.ex-opt-btn').forEach(btn => {
          btn.addEventListener('click', () => {
            const chosen = btn.dataset.val;
            const isCorrect = chosen === q.correct;
            if (isCorrect) correct++;

            // Disable all buttons and colour them
            app.querySelectorAll('.ex-opt-btn').forEach(b => {
              b.disabled = true;
              if (b.dataset.val === q.correct) b.classList.add('correct');
              else if (b === btn && !isCorrect) b.classList.add('wrong');
            });

            const fb = document.getElementById('gf-feedback');
            fb.style.display = 'block';
            fb.innerHTML = `
              <p class="${isCorrect ? 'fb-correct' : 'fb-wrong'}">${isCorrect ? '✓ Correct!' : `✗ The answer is <strong>${q.correct}</strong>`}</p>
              <p class="fb-explanation">${q.explanation}</p>
              <button class="ex-btn ex-btn-primary" id="gf-next">
                ${idx + 1 < questions.length ? 'Next sentence →' : 'See results →'}
              </button>
            `;
            document.getElementById('gf-next').addEventListener('click', () => { idx++; showQuestion(); });
          });
        });
      }

      showQuestion();
    })
    .catch(() => {
      app.innerHTML = '<p class="error">Could not load gap-fill data.</p>';
    });
}

function showGapFillSummary(app, correct, total) {
  const pct = Math.round((correct / total) * 100);
  app.innerHTML = `
    <div class="ex-summary">
      <div class="ex-summary-icon">${pct >= 70 ? '✓' : '→'}</div>
      <h2>${correct}/${total} correct</h2>
      <p>${pct >= 70 ? 'Strong work — you can read Latin in context.' : 'Keep going — context reading takes practice.'}</p>
      <div class="ex-summary-actions">
        <a href="quiz.html?activity=gap-fill" class="ex-btn ex-btn-primary">Try again</a>
        <a href="quiz.html" class="ex-btn ex-btn-secondary">Back to Exercises</a>
      </div>
    </div>
  `;
}
```

Add route:
```javascript
case 'gap-fill': return runGapFill(app);
```

Add to hub in `renderHub()`:
```javascript
{
  id: 'gap-fill',
  title: 'Gap-Fill Sentences',
  desc: 'Complete a Latin sentence by choosing the right word.',
  ce: 'Practises: CE Questions 1 & 2 (context reading)',
  href: 'quiz.html?activity=gap-fill',
  icon: '✏️'
}
```

Also add `shuffle()` helper if not already present in exercises.js:
```javascript
function shuffle(arr) {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}
```

Add CSS to `css/style.css` for new components:
```css
/* ============================================
   Exercise Activities — Phase 3 (03-03 additions)
   ============================================ */

/* Matching Pairs */
.pairs-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.5rem;
  margin: 1rem 0;
}

.pairs-col {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.pair-btn {
  width: 100%;
  padding: 0.6rem 0.5rem;
  background-color: var(--color-parchment-dark);
  border: 2px solid var(--color-border);
  border-radius: 4px;
  font-family: var(--font-body);
  font-size: 0.95rem;
  color: var(--color-stone);
  cursor: pointer;
  transition: border-color 0.15s, background-color 0.15s;
  text-align: center;
}

.pair-btn.selected {
  border-color: var(--color-terracotta);
  background-color: rgba(181, 69, 27, 0.08);
}

.pair-btn.matched {
  border-color: #3a7a3a;
  background-color: rgba(58, 122, 58, 0.12);
  color: #2a5a2a;
  cursor: default;
}

.pair-btn.wrong {
  border-color: var(--color-terracotta);
  background-color: rgba(181, 69, 27, 0.12);
}

.pairs-score {
  font-size: 0.9rem;
  color: var(--color-stone-light);
  text-align: center;
  margin-top: 0.5rem;
}

.ex-topic-label {
  font-size: 0.9rem;
  color: var(--color-stone-light);
  margin-bottom: 1rem;
}

/* Gap-Fill */
.ex-sentence-card {
  background-color: var(--color-parchment-dark);
  border: 1px solid var(--color-border);
  border-radius: 4px;
  padding: 1rem 1.25rem;
  margin-bottom: 1rem;
}

.ex-sentence {
  font-size: 1.2rem;
  font-style: italic;
  margin-bottom: 0.25rem;
}

.ex-translation-hint {
  font-size: 0.9rem;
  color: var(--color-stone-light);
  margin: 0;
}
```

**Verify:**
- `quiz.html?activity=pairs` shows matching pairs with two columns
- `quiz.html?activity=gap-fill` shows Latin sentence with blank and 4 options
- Gap-fill correct answer shows explanation text
- Gap-fill wrong answer shows correct form + explanation
- After 8 gap-fill questions, summary shows score

**Done:** Matching pairs and gap-fill activities complete. Both activities use real data (vocabulary JSON and gap-fill JSON), with grammatical explanations after each gap-fill answer.

---

## must_haves

- `quiz.html?activity=pairs` shows two columns of 6 Latin/English buttons that match when tapped
- Correct pair locks green; wrong pair flashes red and resets
- All 6 matched shows success screen
- `quiz.html?activity=gap-fill` shows a Latin sentence with one blank and 4 word options
- After answering gap-fill, explanation shows why the correct word/form is used
- `data/exercises/gap-fill.json` exists with at least 20 sentences and is valid JSON
- Both activities listed on exercise hub
- Both activities work fully offline
