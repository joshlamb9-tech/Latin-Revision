---
phase: 01-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/style.css
  - js/app.js
  - icons/icon-192.png
  - icons/icon-512.png
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]

must_haves:
  truths:
    - "The site loads at index.html and displays the classical visual identity (EB Garamond, parchment/terracotta palette)"
    - "The hamburger nav works on mobile — menu opens and closes, links are tappable"
    - "All page stubs (index, vocabulary, grammar, quiz, papers) are reachable via nav"
    - "Macron characters render correctly in the browser (ā ē ī ō ū — not ? or boxes)"
    - "The layout is mobile-first — usable on a 375px iPhone without horizontal scroll"
  artifacts:
    - path: "index.html"
      provides: "App shell with main#app pattern, nav, meta tags"
      contains: "id=\"app\""
    - path: "css/style.css"
      provides: "Classical visual theme — EB Garamond, CSS custom properties for parchment palette"
      contains: "--color-parchment"
    - path: "js/app.js"
      provides: "Nav toggle, SW registration hook, page routing stub"
      contains: "hamburger"
  key_links:
    - from: "index.html"
      to: "css/style.css"
      via: "<link rel=\"stylesheet\"> in <head>"
      pattern: "stylesheet"
    - from: "index.html"
      to: "js/app.js"
      via: "<script src=\"js/app.js\"> before </body>"
      pattern: "app.js"
    - from: "js/app.js"
      to: "main#app"
      via: "document.getElementById('app') — all content rendered here"
      pattern: "getElementById.*app"
---

<objective>
Build the app shell: HTML structure, classical visual identity, mobile-first responsive layout with hamburger navigation.

Purpose: Establishes the visual identity and structural pattern (page-as-shell with main#app) that every subsequent page inherits. Shell and data layer are independent — this plan runs in parallel with 01-01.
Output: index.html, css/style.css, js/app.js — a working, styled, navigable shell with page stubs.
</objective>

<execution_context>
@/Users/josh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md

## Design direction
- Font: EB Garamond from Google Fonts (https://fonts.google.com/specimen/EB+Garamond)
- Palette: parchment background (#F5ECD7 or similar warm off-white), terracotta accent (#B5451B or similar), stone text (#3A3530 or similar dark warm grey)
- NOT blue, NOT Dosis — this is a Latin site, not the French site
- Roman-feeling but not costumey — clean, classical, legible
- Think: old manuscript meets clean web — not Comic Parchment, not Halloween

## Architecture pattern
- index.html is a minimal shell — `<main id="app">` populated by JS
- Nav links to stub pages (vocabulary.html, grammar.html, quiz.html, papers.html)
- Each stub follows the same shell pattern
- UTF-8 meta tag MUST be the first tag in <head> (before any other tags)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html and page stubs</name>
  <files>index.html, vocabulary.html, grammar.html, quiz.html, papers.html</files>
  <action>
    Create index.html as the primary shell and four stub pages. All files must use identical structure — content in main#app only.

    ## index.html

    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <meta name="description" content="CE Latin revision — vocabulary, grammar, and practice for ISEB Common Entrance">
      <meta name="theme-color" content="#B5451B">
      <title>CE Latin Revision</title>
      <link rel="manifest" href="manifest.webmanifest">
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,500;0,700;1,400&display=swap" rel="stylesheet">
      <link rel="stylesheet" href="css/style.css">
    </head>
    <body>
      <header class="site-header">
        <div class="header-inner">
          <a href="index.html" class="site-title">CE Latin</a>
          <button class="nav-toggle" aria-label="Toggle navigation" aria-expanded="false">
            <span class="hamburger-bar"></span>
            <span class="hamburger-bar"></span>
            <span class="hamburger-bar"></span>
          </button>
          <nav class="site-nav" id="site-nav">
            <ul>
              <li><a href="index.html">Home</a></li>
              <li><a href="vocabulary.html">Vocabulary</a></li>
              <li><a href="grammar.html">Grammar</a></li>
              <li><a href="quiz.html">Quiz</a></li>
              <li><a href="papers.html">Past Papers</a></li>
            </ul>
          </nav>
        </div>
      </header>
      <main id="app">
        <section class="hero">
          <h1>CE Latin Revision</h1>
          <p class="hero-sub">Vocabulary, grammar, and practice for ISEB Common Entrance Latin</p>
        </section>
        <section class="home-nav-grid">
          <a href="vocabulary.html" class="nav-card">
            <h2>Vocabulary</h2>
            <p>ISEB CE word list — browse and search</p>
          </a>
          <a href="grammar.html" class="nav-card">
            <h2>Grammar</h2>
            <p>Declension and conjugation tables</p>
          </a>
          <a href="quiz.html" class="nav-card">
            <h2>Quiz</h2>
            <p>Test yourself — Latin to English</p>
          </a>
          <a href="papers.html" class="nav-card">
            <h2>Past Papers</h2>
            <p>ISEB specimen and past papers</p>
          </a>
        </section>
      </main>
      <footer class="site-footer">
        <p>CE Latin Revision &middot; For ISEB Common Entrance &middot; Content sourced from ISEB syllabus and Bob Bass materials (used with permission)</p>
      </footer>
      <script src="js/app.js"></script>
    </body>
    </html>
    ```

    Create vocabulary.html, grammar.html, quiz.html, papers.html with the SAME header/nav/footer structure, but main#app contains only a heading and a placeholder paragraph — content is rendered by JS in Phase 2/3.

    Example stub main content for vocabulary.html:
    ```html
    <main id="app">
      <h1>Vocabulary</h1>
      <p>Loading vocabulary list&hellip;</p>
    </main>
    ```

    IMPORTANT encoding rules:
    - charset="UTF-8" MUST be the very first tag in <head>
    - Use &hellip; &middot; etc as HTML entities in HTML, but actual Unicode (ā ē ī ō ū) in any Latin text content
    - lang="en" on <html>
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); ['index.html','vocabulary.html','grammar.html','quiz.html','papers.html'].forEach(f => { const c = fs.readFileSync('/Users/josh/projects/ce-latin-revision/' + f, 'utf8'); const ok = c.includes('id=\"app\"') && c.includes('charset=\"UTF-8\"') && c.includes('app.js'); console.log(f, ok ? 'OK' : 'MISSING REQUIRED ELEMENTS'); });"</automated>
  </verify>
  <done>Five HTML files exist. All contain id="app", charset="UTF-8" as first meta tag, nav with all five links, and a script tag loading js/app.js.</done>
</task>

<task type="auto">
  <name>Task 2: Create CSS visual identity and JS nav toggle</name>
  <files>css/style.css, js/app.js</files>
  <action>
    ## css/style.css

    Create css/ directory and style.css with the classical visual theme.

    Design system using CSS custom properties:

    ```css
    /* Palette */
    --color-parchment: #F5ECD7;       /* page background */
    --color-parchment-dark: #EAD9B8;  /* card/table backgrounds */
    --color-terracotta: #B5451B;      /* primary accent — headings, links, borders */
    --color-terracotta-dark: #8C3315; /* hover states */
    --color-stone: #3A3530;           /* body text */
    --color-stone-light: #6B6259;     /* secondary text, captions */
    --color-border: #C9B896;          /* subtle borders */

    /* Typography */
    --font-body: 'EB Garamond', Georgia, serif;
    --font-size-base: 1.1rem;         /* slightly large for exam revision readability */
    --line-height-base: 1.65;
    ```

    Required styles:
    1. Reset: box-sizing: border-box; margin: 0; padding: 0 on *, body
    2. Body: background-color: var(--color-parchment); color: var(--color-stone); font-family: var(--font-body); font-size: var(--font-size-base); line-height: var(--line-height-base)
    3. Site header: background terracotta, parchment text, sticky top, z-index 100
    4. Header inner: flex row, space-between, max-width ~900px, centered, padding 0 1rem
    5. Site title (logo): EB Garamond, parchment colour, font-size 1.4rem, no underline
    6. Hamburger button (.nav-toggle): 44x44px tap target (iOS minimum), display:flex, flex-direction:column, gap:5px, no border, terracotta background on header
    7. Hamburger bars (.hamburger-bar): 22px wide, 2px tall, background parchment
    8. Nav (.site-nav): hidden by default on mobile (display:none). When .open class is on nav, display:block. Position absolute under header, full width, background terracotta-dark, z-index 99
    9. Nav ul: no list-style, padding 0
    10. Nav li a: display:block, padding:1rem 1.5rem, color parchment, no underline, hover background terracotta
    11. Desktop (min-width:768px): .nav-toggle display:none; .site-nav display:block; nav positioned normally (not absolute); nav ul flex row; li a padding:0.5rem 1rem
    12. Main#app: max-width:900px; margin:0 auto; padding:1.5rem 1rem
    13. Hero section: text-align:center; padding:2rem 0
    14. h1: font-size:2rem; color terracotta; margin-bottom:0.5rem
    15. .hero-sub: font-size:1.1rem; color stone-light
    16. Home nav grid: display:grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap:1rem; margin-top:2rem
    17. .nav-card: display:block; background parchment-dark; border:1px solid border colour; border-radius:4px; padding:1.25rem; text-decoration:none; color stone; transition:border-color 0.2s
    18. .nav-card:hover: border-color terracotta; color stone
    19. .nav-card h2: font-size:1.2rem; color terracotta; margin-bottom:0.25rem
    20. .nav-card p: font-size:0.95rem; color stone-light; margin:0
    21. Site footer: background parchment-dark; border-top 1px solid border; padding:1rem; text-align:center; font-size:0.85rem; color stone-light; margin-top:3rem

    ## js/app.js

    Create js/ directory and app.js. Responsibilities:
    1. Hamburger nav toggle
    2. Close nav when a nav link is clicked (mobile UX)
    3. Service worker registration hook (calls registerSW() — defined in sw.js; gracefully skips if SW not yet present)

    ```javascript
    'use strict';

    // --- Nav toggle ---
    const navToggle = document.querySelector('.nav-toggle');
    const siteNav = document.getElementById('site-nav');

    if (navToggle && siteNav) {
      navToggle.addEventListener('click', () => {
        const isOpen = siteNav.classList.toggle('open');
        navToggle.setAttribute('aria-expanded', String(isOpen));
      });

      // Close nav when a link is tapped (mobile)
      siteNav.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
          siteNav.classList.remove('open');
          navToggle.setAttribute('aria-expanded', 'false');
        });
      });
    }

    // --- Service Worker registration ---
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('sw.js')
          .then(reg => console.log('[CE Latin] SW registered, scope:', reg.scope))
          .catch(err => console.warn('[CE Latin] SW registration failed:', err));
      });
    }
    ```

    The SW registration will silently fail if sw.js doesn't exist yet — that is fine and expected until Plan 01-03.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const css = fs.readFileSync('/Users/josh/projects/ce-latin-revision/css/style.css', 'utf8'); const js = fs.readFileSync('/Users/josh/projects/ce-latin-revision/js/app.js', 'utf8'); console.log('CSS custom props:', css.includes('--color-parchment') && css.includes('--color-terracotta')); console.log('CSS EB Garamond:', css.includes('EB Garamond')); console.log('CSS mobile nav:', css.includes('.site-nav') && css.includes('.open')); console.log('CSS hamburger:', css.includes('.nav-toggle')); console.log('JS nav toggle:', js.includes('nav-toggle') && js.includes('classList.toggle')); console.log('JS SW register:', js.includes('serviceWorker') && js.includes('register'));"</automated>
    <manual>Open index.html in a browser. Check: EB Garamond loads, parchment background, terracotta header, hamburger button visible at 375px viewport width, clicking hamburger opens nav, clicking a nav link closes nav.</manual>
  </verify>
  <done>css/style.css has the full classical theme with all required custom properties. js/app.js has working nav toggle and SW registration stub. Opening index.html in a browser shows the classical parchment/terracotta visual identity with functional hamburger nav on mobile viewport.</done>
</task>

</tasks>

<verification>
Open index.html in browser at 375px viewport width. Verify: EB Garamond font loads, parchment background, terracotta header and accent colours, hamburger button visible and functional, nav opens and closes, four nav cards on home page, footer visible. Resize to 768px+ and verify hamburger disappears and nav goes horizontal.
</verification>

<success_criteria>
- index.html and four page stubs exist, all follow the shell pattern with id="app"
- UTF-8 charset is the first tag in every HTML file's head
- EB Garamond loads from Google Fonts
- Parchment/terracotta/stone colour palette applied via CSS custom properties
- Hamburger nav works on mobile, collapses to horizontal on desktop
- No horizontal scroll at 375px viewport width
- js/app.js registers service worker (graceful failure if sw.js absent)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` covering:
- Final colour palette hex values chosen
- Any deviations from the design spec above (and why)
- Screenshot description of the home page on mobile
- Files created and their responsibilities
</output>
